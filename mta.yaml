_schema-version: "3.2"
ID: comtrainingexer3pebenito
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: comtrainingexer3pebenito-destination-content
  type: com.sap.application.content
  requires:
  - name: comtrainingexer3pebenito-destination-service
    parameters:
      content-target: true
  - name: comtrainingexer3pebenito-repo-host
    parameters:
      service-key:
        name: comtrainingexer3pebenito-repo-host-key
  - name: comtrainingexer3pebenito-uaa
    parameters:
      service-key:
        name: comtrainingexer3pebenito-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: comtrainingexer3pebenito_html_repo_host
          ServiceInstanceName: comtrainingexer3pebenito-html5-service
          ServiceKeyName: comtrainingexer3pebenito-repo-host-key
          sap.cloud.service: comtrainingexer3pebenito
        - Authentication: OAuth2UserTokenExchange
          Name: comtrainingexer3pebenito_uaa
          ServiceInstanceName: comtrainingexer3pebenito-xsuaa-service
          ServiceKeyName: comtrainingexer3pebenito-uaa-key
          sap.cloud.service: comtrainingexer3pebenito
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: comtrainingexer3pebenito-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: comtrainingexer3pebenito-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - comtrainingexer3pebenito.zip
      name: comtrainingexer3pebenito
      target-path: resources/
- name: comtrainingexer3pebenito
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: comtrainingexer3pebenito-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: comtrainingexer3pebenito-destination-service
    service-plan: lite
- name: comtrainingexer3pebenito-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: comtrainingexer3pebenito-xsuaa-service
    service-plan: application
- name: comtrainingexer3pebenito-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: comtrainingexer3pebenito-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
